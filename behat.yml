# behat.yml

default:
    suites:
        base_features:
            paths:    [ %paths.base%/features/Base ]
            contexts: [ FeatureContext ]
    formatters:
        pretty: true
        junit: true
